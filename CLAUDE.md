# claude-code-ollama

Claude Code をローカルの Ollama モデルに向けるランチャ。

## このリポジトリは public

**書いたものは全世界に公開され、git 履歴に永久に残る。**
visibility を後から下げても、公開されていた事実も、SHA 指定で取れるコミットも消えない。

**コミットする前に、追加した行を1つずつ見て機密性を確かめること。**

### 書いてはいけないもの

* **作者のマシン固有の絶対パス**（`E:\...`, `C:\Users\<名前>\...`）。
  README では `/path/to/claude-code-ollama` のようなプレースホルダを使う
* **作者が実際に使っているモデル名**。特に uncensored / abliterated 系や、
  個人の嗜好が読み取れるもの。**設定例には公式の open-weight モデルを使う**
* **private リポジトリへのリンク**（外から 404 になるうえ、存在と名前が漏れる）
* **`~/.claude/` の中身への参照**（`~/.claude/docs/xxx.md` を見ろ、等）。
  必要な内容はこのリポジトリ内に書き切る
* 認証情報、API キー、その置き場

### 書いてよいもの

* **ハードウェア構成**（`RTX 5060 Ti 16GB` / `PCIe 3.0 x8` / `RAM 32GB` 等）。
  **これが無いと実測値が読めない**ので、むしろ必要
* ソフトウェアの版数、実測値、踏んだ罠、外した仮説
* GitHub のユーザー名（既に public）

**迷ったら書かない。** 判断がつかないものは、private 側（issue）に置く。

## issue はこのリポジトリに無い

**作業中の課題・調査記録は private リポジトリ側にある。**
このリポジトリは**成果物だけ**を置く。

未解決の課題をここに issue として立てないこと
（issue 本文にも個人的な文脈が入りやすい）。

## 実測値の扱い

**数値には計測日と機材を添える。** 環境が違えば結論も変わる。

「判断を左右するなら測り直すこと」を明記する。読み手が
その数字を信じてよいか判断できるようにする。

## 外した仮説を消さない

README には「最初こう判断したが外れた」という記録がいくつか入っている。
**これは冗長ではなく、同じ罠を踏む人への一番の情報。**
整理するときに消さないこと。
